# The Agencia x402 service this platform consumes
AGENCIA_SERVICE_URL=http://localhost:3022

# Hedera network the consuming agent pays on
HEDERA_NETWORK=hedera:testnet
HEDERA_MIRROR_NODE_URL=https://testnet.mirrornode.hedera.com
HASHSCAN_BASE=https://hashscan.io/testnet

# Consuming agent wallet (server-side only — never exposed to the browser)
AGENT_ACCOUNT_ID=0.0.xxxxxxx
AGENT_PRIVATE_KEY=
AGENT_BUDGET_HBAR=5
